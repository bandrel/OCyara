sudo: required

services:
  - docker
  
language: python

python:
  - "3.6"
before_install:
  - docker build -t bandrel/ocyara .
install: "docker run -it bandrel/ocyara /bin/bash"
# command to run tests
script: pytest  
